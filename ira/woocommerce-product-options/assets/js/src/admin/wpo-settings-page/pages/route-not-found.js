import { __ } from '@wordpress/i18n';

const RouteNotFound = () => {
	return <div id="barn2-pages-wrapper">{ __( 'Route not found.', 'woocommerce-product-options' ) }</div>;
};

export default RouteNotFound;
